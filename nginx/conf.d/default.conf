## Minimal default.conf used by nginx:alpine entrypoint scripts
## Keep this file simple to avoid template modifications breaking our config.

types_hash_max_size 4096;

## Include the real proxy config
include /etc/nginx/conf.d/hefaistos.conf;
